{
  "name": "src",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cross-env NODE_PATH=. node ./bin/www",
    "debug": "cross-env NODE_PATH=. node --debug-brk --inspect ./bin/www",
    "test": "cross-env NODE_PATH=. tape ./test/**/*-test.js",
    "heroku-postbuild": "cd client && yarn --production=false && yarn run build"
  },
  "dependencies": {
    "body-parser": "~1.18.2",
    "checkenv": "^1.2.2",
    "cookie-parser": "~1.4.3",
    "cross-env": "^5.1.1",
    "debug": "~2.6.9",
    "dotenv": "^4.0.0",
    "express": "~4.15.5",
    "jade": "~1.11.0",
    "morgan": "~1.9.0",
    "serve-favicon": "~2.4.5",
    "supertest": "^3.0.0",
    "tape": "^4.8.0",
    "yarn": "^1.3.2"
  }
}
